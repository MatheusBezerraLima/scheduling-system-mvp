{
  "name": "scheduling-system-mvp",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "db:migrate": "dotenv -e .env -- npx sequelize-cli db:migrate",
    "db:seed": "dotenv -e .env -- npx sequelize-cli db:seed:all",
    "db:seed:undo": "dotenv -e .env -- npx sequelize-cli db:seed:all:undo",
    "db:migrate:undo": "dotenv -e .env -- npx sequelize-cli db:migrate:undo",
    "db:migrate:undo:all": "dotenv -e .env -- npx sequelize-cli db:migrate:undo:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3",
    "postgres": "^3.4.7",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "sequelize-cli": "^6.6.3"
  }
}
